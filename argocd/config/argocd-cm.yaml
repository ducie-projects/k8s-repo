apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-cm
    app.kubernetes.io/part-of: argocd
data:
  # Add repository credentials if needed
  repositories: |
  # Example:
  # - type: git
  #   url: https://github.com/ducie-projects/k8s-repo
  # Customize other ArgoCD settings as needed
